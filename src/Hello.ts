export function add(n:number, m:number):number {
    return n + m;
}

export function sub(n:number, m:number):number {
    return n - m;
}

export function pythag(n:number, m:number):number {
    return Math.sqrt((n * n) + (m * m))
}